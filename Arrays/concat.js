// array method - concat - adds multiple elements and RETURNS A COPY

let myName = 'seth'
let myHobby = 'likes to code.'

let phrase = myName + ' ' + myHobby
console.log(phrase)

let samePhrase = myName.concat(myHobby)
console.log(samePhrase)

// works the same for arrays

let myArray = ['a', 'b', 'c']
let secondHalf = []
